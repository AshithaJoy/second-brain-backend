export const getHooksUserPromptV1 = (title: string, mood: string) => `
Generate structured hook variations and creator assets for a post titled "${title}" with a "${mood}" mood.
Ensure the hooks are highly relevant to content creators, slow living, minimalist productivity, or building in public.
`;
